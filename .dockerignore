# 版本控制 / IDE / 本地状态
.git
.gitignore
.claude
.agents
.vscode
.idea

# 文档与截图（构建不需要）
docs
images
*.md

# 依赖与构建产物（镜像内重新安装/构建）
**/node_modules
**/dist
client-vue/apps/web-antd/dist.zip

# 本地数据与密钥（运行时通过 -v / -e 注入，绝不进镜像）
**/.env
**/.env.*
!**/.env.example
*.sqlite
*.sqlite3
data

# 日志与临时文件
**/*.log

# Docker 自身
Dockerfile
.dockerignore
docker-compose.yml
